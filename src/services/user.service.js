const { User } = require("../models/user");

const addUserService = async (userInput) => {
  // Define addUser service
};

const getUserProfileService = async (userInput) => {
  // Define getUserProfile service
};

module.exports = { addUserService, getUserProfileService };
